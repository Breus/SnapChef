# Uncomment the following 2 lines if you want to use quarkusDev instead of the Docker container
# Comment the following 2 lines if you want to run the Quarkus application in a Docker container
services:
  quarkus: !reset null